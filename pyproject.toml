[project]
name = "lm-studio-remote"
version = "0.1.0"
description = "LM Studio remote tui"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "httpx>=0.28.1",
    "textual>=8.2.8",
]
